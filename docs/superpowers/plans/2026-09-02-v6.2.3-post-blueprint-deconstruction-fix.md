# V6.2.3 Implementation Plan

1. Add failing regression tests for wrong seed layout, nonstandard core border,
   and inherited effects.
2. Add a saved-PPTX output contract covering template, skeleton, and all effect
   scopes.
3. Extend the macOS OOXML scrubber for inherited effect references and explicit
   inner/outer shadows.
4. Run the contract before deconstruction acceptance and bind its report hash to
   cached reuse.
5. Validate the real Windows COM build, the real cross-platform python-pptx
   build, the focused suite, and the complete suite; then publish main and sync
   the local global skill without deleting unrelated local changes.
