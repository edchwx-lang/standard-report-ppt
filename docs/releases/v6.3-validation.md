# Standard Report PPT V6.3 验证记录

## 升级边界

- 基线：V6.2.2（`d3c9103`）。
- 只修改正式蓝图锁定后的 `deconstruct` 路径。
- 蓝图生成前流程、ImageGen 输入/输出、蓝图字节与 `bitmap` 路径保持不变。
- Windows 主路线使用 PowerPoint COM；macOS 迁移同一场景图语义，使用 python-pptx/OOXML。
- 本轮不安装到本地全局，不上传，不创建发布标签。

## S3 实际验证

- 输入：用户提供并视为已锁定的 S3 蓝图 PNG。
- 运行链：重叠视觉切片 → 独立视觉普查 → 原子场景图 → 局部裁剪 → PowerPoint COM 重建 → 原生渲染 → 编辑性/结构/视觉验收。
- 主体以蓝图为最高视觉权威；章节、标题、核心判断、资料来源、页码五部分沿用母版，只替换文字。
- 视觉普查候选：85。
- 场景元素：85。
- 可编辑主体对象：69。
- 合规局部图片裁剪：16。
- 原母版占位符：5。
- 幻灯片对象总数：90。
- 完整页面位图：0。
- 接受状态：通过，结构阻断项 0。
- 蓝图保真分：0.7096；布局分：0.6300；墨量比：0.9483。
- PPTX SHA-256：`14c38db57d814ea3516cc21e33dfb484aa4c9efa74b88155ac27dff25fb0b936`。

第一次输出后只进行了规定内的一次定向重构：去除核心判断的重复项目符号，补齐三个区域说明的省名，修正文案中的连接号，并把增长箭头从直线连接符改为可编辑自由形状箭头。第二次结构与保真验收通过，流程直接交付，没有继续循环。

已知且接受的差异：上三层与页脚两部分按公司母版呈现，因此不会像素复刻蓝图中的对应骨架；曲线增长箭头用可编辑自由形状近似。未观察到全页位图、图片重影或局部图片未裁剪问题。

## 自动化验证

- `python tests/run_v63_e2e.py`：10 项通过，包含 Windows V6.3 端到端运行。
- macOS/V6.3 结构测试：17 项通过；真实 PowerPoint for Mac 原生渲染仍标记为 `mac_native_render_unverified`。
- `python -m unittest discover -s tests -q` 等价收集运行：456 项通过，0 失败，0 错误，0 跳过。
