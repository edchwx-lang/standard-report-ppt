# V6.3 TEST.docx 全流程实测记录

本次在提交 `64de432` 后，以 `TEST.docx` 新建两页 `deconstruct` 项目，完整执行结构化解析、规范化内容、提示词校验、逐页 ImageGen、蓝图锁定、V6.3 视觉普查、原子场景图、局部裁剪、PowerPoint COM 构建、原生渲染、保真比较和最终验收。

结果：两页 PPTX 与渲染均生成，104 个主体对象可编辑、8 个复杂视觉为局部裁剪；S01/S02 保真分分别为 0.5305/0.6582并通过阈值。最终验收因 14 个点集元素的 PowerPoint 实际包围盒与场景图声明不一致而阻断，未打包正式交付 ZIP。

实测同时暴露：COM 富文本 `runs` 在 `TextRange2.Characters()` 失败；V6.3 灾难性修复错误走旧版快照校验；场景图缺少 `bbox_px` 与 `points_px` 极值一致性前置校验。其中恢复快照路由已以测试驱动方式修复并提交为 `cf3c954`，另外两项保留为后续修复候选。

测试项目及完整证据位于 `C:/Users/edchw/Documents/START PPT/TEST_v63_full_2p`。

代码回归结果：V6.3 专项端到端 10/10 通过；全量自动化测试 457/457 通过，0 失败、0 错误、0 跳过。
