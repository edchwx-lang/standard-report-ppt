# Legacy auditable blueprint deconstruction prompt

This prompt is retained only for an explicitly requested audit/geometry workflow. It is not part of V5 Direct Blueprint and must never be inserted into the default production path.

For Direct Blueprint, inspect each accepted page visually and encode the reconstruction directly in that page’s `build_slide_SNN` function plus bounded `ASSET_CROPS`. Do not create `blueprint_geometry.json` merely to satisfy a historical contract.

If the user explicitly requests auditable geometry artifacts, follow the V4 compatibility validator and label the project as the auditable engine. Do not claim that compatibility geometry is Direct Blueprint.
