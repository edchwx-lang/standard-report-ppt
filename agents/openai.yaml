interface:
  display_name: "Standard Report PPT"
  short_description: "V6.3.1 editable reconstruction or bitmap report decks"
  brand_color: "#2F5597"
  default_prompt: "Use $standard-report-ppt V6.3.1 to create a report deck from my material; confirm the exact page count and bitmap or deconstruct mode if missing, then follow SKILL.md for blueprint generation, master-preserving reconstruction, verification and delivery."
policy:
  allow_implicit_invocation: true
