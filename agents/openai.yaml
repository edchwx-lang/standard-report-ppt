interface:
  display_name: "Standard Report PPT"
  short_description: "V5.9.6 cross-platform blueprint-aligned editable consulting decks"
  brand_color: "#2F5597"
default_prompt: "Use $standard-report-ppt V5.9.6 with V5.8 compatibility; confirm the exact page count and mode, use the real first slide as the ImageGen capability probe, lock one immutable blueprint per page, inspect the full page plus Q1-Q4 review tiles, enforce mandatory crop contracts, select windows_com_v584 on Windows or mac_python_pptx_v1 on macOS, build locally once, and package only after the backend-specific quality and asset audits pass."
policy:
  allow_implicit_invocation: true
